export interface RecordingDataDef {
  title: string;
  id: string;
  shortDescription: string;
}

export interface RecordingData {
  title: string;
  id: string;
  shortDescription: string;
  content: string;
  slug: string;
}
